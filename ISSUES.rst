ISSUES:

1) Redundant code
2) Misleading variable names
3) No comments
4) If the searched string doesn't have a wiki-database, it doesn't return the title of the first link displayed on wiki search suggestion page.